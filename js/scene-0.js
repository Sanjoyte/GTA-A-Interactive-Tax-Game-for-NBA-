/* ============================================================
   #SCENE 0 — WELCOME
   ============================================================ */


/* ============================================================
   ELEMENTS
   ============================================================ */

const scene0Start =
    document.getElementById(
        "scene-0-start"
    );


/* ============================================================
   START FROM WELCOME
   ============================================================ */

scene0Start.addEventListener(
    "click",
    () => {

        console.log(
            "Opening video from welcome screen."
        );

        openScene1();

    },
    {
        once: true
    }
);

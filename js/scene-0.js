/* ============================================================
   #SCENE 0 — WELCOME
   ============================================================ */


/* ============================================================
   ELEMENTS
   ============================================================ */

const scene0 =
    document.getElementById(
        "scene-0"
    );


/* ============================================================
   START FROM WELCOME
   ============================================================ */

scene0.addEventListener(
    "click",
    () => {

        console.log(
            "Opening video from welcome screen."
        );

        openScene1();

    },
    {
        once: true
    }
);
